export enum PetitionStatus {
    Pending = "pending",
    Accepted = "accepted",
    Declined = "declined"
}
export enum PetitionModelType{
    match = "Match",
    group = "Group",
    friend = "User"
}